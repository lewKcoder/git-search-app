import styled from "@emotion/styled";

export const StyledDivInputWrapper = styled.div({
  border: "1px solid #c4c4c4",
  borderRadius: "3px",
  display: "inline-block",
});

export const StyledInput = styled.input({
  border: "none",
  fontSize: "16px",
  outline: "none",
  padding: "4px 8px",
});

export const StyledButton = styled.button({
  background: "#1980e5",
  border: "none",
  color: "#fff",
  cursor: "pointer",
  fontSize: "16px",
  height: "26.5px",
});
